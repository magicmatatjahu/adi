export function customWrapper() {

}

/*
 * Something like @Inject(Optional()) private readonly lol: Service
 * or 
 * @Inject(Optional(Service)) private readonly lol: any
 * 
 * or even
 * @Inject(All(Decorate(Scoped()))) private readonly lol: Service
 */
